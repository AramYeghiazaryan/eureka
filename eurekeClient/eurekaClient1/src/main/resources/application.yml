server:
  port: 8093

eureka:
  instance:
    statusPageUrlPath: /info
    healthCheckUrlPath: /health
  client:
    service-url:
      defaultZone: http://localhost:7070/eureka

spring:
  application:
    name: EurekaClient